//! Action dispatcher for canvas operations

/// Dispatches actions on a canvas
#[derive(Debug)]
pub struct ActionDispatcher {
    // Placeholder implementation
}

impl ActionDispatcher {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for ActionDispatcher {
    fn default() -> Self {
        Self::new()
    }
}